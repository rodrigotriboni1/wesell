<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wesell">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wesell"
        android:enableOnBackInvokedCallback="true">

        <!-- Defina a LoginActivity como a atividade de lançamento padrão -->
        <activity
            android:name=".cadastro.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras atividades do seu aplicativo -->
        <activity android:name=".cliente.ClienteActivity"></activity>
        <activity android:name=".cadastro.RegisterActivity"></activity>
        <activity android:name=".venda.AddVendaActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".detalhes.DetalhesProdutoActivity"></activity>
        <activity android:name=".produtos.ProdutoActivity"></activity>
        <activity android:name=".cadastro.LoginActivity"></activity>

    </application>

</manifest>
